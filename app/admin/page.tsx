import { desc } from "drizzle-orm";
import { redirect } from "next/navigation";
import { getDb } from "@/db";
import { facebookUsers, searches } from "@/db/schema";
import { isAdmin } from "@/lib/admin-auth";
import { eq } from "drizzle-orm";
export const dynamic="force-dynamic";
export default async function Admin(){if(!(await isAdmin()))redirect("/admin/login");const db=getDb();const rows=await db.select({search:searches,facebookName:facebookUsers.name,facebookId:facebookUsers.facebookId,profileLink:facebookUsers.profileLink}).from(searches).leftJoin(facebookUsers,eq(searches.userId,facebookUsers.userId)).orderBy(desc(searches.createdAt)).limit(200);return <main className="adminShell"><header><div><small>لوحة خاصة</small><h1>إدارة فتّاشة</h1></div><form action="/api/admin/logout" method="post"><button className="secondary">تسجيل الخروج</button></form></header><section className="adminStats"><article><b>{rows.length}</b><span>آخر عمليات البحث</span></article><article><b>{rows.reduce((n,r)=>n+r.search.searchedSources,0)}</b><span>عمليات فحص المصادر</span></article><article><b>{rows.filter(r=>r.facebookId).length}</b><span>عمليات مرتبطة بفيسبوك</span></article></section><section className="adminTable"><h2>الصور وعمليات البحث</h2><div className="adminGrid">{rows.map(({search:row,facebookName,facebookId,profileLink})=><article key={row.id}><a className="adminThumb" href={`/api/admin/image/${row.id}`} target="_blank" rel="noreferrer" title="فتح الصورة بالحجم الكامل"><img src={`/api/admin/image/${row.id}`} alt="الصورة المرفوعة"/></a><div><b>{row.filename}</b><span dir="ltr">{row.userId}</span>{facebookName&&<><strong>{facebookName}</strong><small dir="ltr">Facebook ID: {facebookId}</small>{profileLink?<a href={profileLink} target="_blank" rel="noreferrer">فتح صفحة فيسبوك</a>:<small>لم تمنح Meta رابط الصفحة</small>}</>}<small>{row.createdAt.toLocaleString("ar-SA")}</small><small>{(row.byteSize/1024/1024).toFixed(2)} MB · {row.contentType}</small><small dir="ltr">IP: غير محفوظ في النسخة الحالية</small><nav className="adminImageActions" aria-label={`إجراءات ${row.filename}`}><a href={`/api/admin/image/${row.id}`} target="_blank" rel="noreferrer">فتح الصورة</a><a href={`/api/admin/image/${row.id}?download=1`} download>حفظ الصورة</a></nav></div></article>)}</div></section></main>}
