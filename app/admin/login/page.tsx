"use client";
import { useState } from "react";
export default function AdminLogin(){const [error,setError]=useState("");const [busy,setBusy]=useState(false);async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setError("");const form=new FormData(e.currentTarget);const response=await fetch("/api/admin/login",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({username:form.get("username"),password:form.get("password")})});const data=await response.json() as {error?:string};if(response.ok)location.href="/admin";else setError(data.error||"تعذّر الدخول");setBusy(false)}return <main className="adminLogin"><form onSubmit={submit}><span>فتّاشة</span><h1>دخول الإدارة</h1><p>هذه الصفحة مخصصة لمالك الموقع فقط.</p><label>اسم المستخدم<input name="username" autoComplete="username" required/></label><label>كلمة المرور<input name="password" type="password" autoComplete="current-password" required/></label><button className="primary" disabled={busy}>{busy?"جارٍ التحقق…":"دخول آمن"}</button>{error&&<div className="errorBox">{error}</div>}</form></main>}
